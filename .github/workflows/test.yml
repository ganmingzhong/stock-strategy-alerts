name: Schedule Test

on:
  workflow_dispatch:
  schedule:
    - cron: '42 14 * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Workflow triggered!"
          date -u
