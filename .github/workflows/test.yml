name: Schedule Test

on:
  workflow_dispatch:
  schedule:
    - cron: '00 02 * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Workflow triggered!"
          date -u
